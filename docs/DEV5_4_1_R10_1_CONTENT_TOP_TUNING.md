# dev.5.4.1 R10.1 - final content-top tuning

The R10 local-coordinate model remains the geometry baseline.

Only the page-local content origin is tightened:

```text
InfoBefore body top = 8 scaled px
SelectDir first-row top = 8 scaled px
```

Header rhythm stays:

```text
Title
8 px
Subtitle
```

Destination body rhythm stays:

```text
first row
16 px
secondary instruction
8 px
path field + Browse
```

Do not move the disk-space label or bottom navigation strip.

If R10.1 is visually accepted in the VM, freeze the installer body layout and
move on to uninstall-branch validation.
