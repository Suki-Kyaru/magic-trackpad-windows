# Raw Patch Notes

This directory contains raw pre-OSS development patch notes moved from the
repository root during OSS-1.1B.

They are intentionally preserved instead of deleted.

Use them when investigating historical implementation details, but prefer
current contract and validation documents under `docs/` for present behavior.
