# dev.5.4.1 R10.2 - final InfoBefore vertical tuning

Final accepted local-coordinate targets:

```text
Header:
Title -> Subtitle = 8 scaled px

InfoBefore:
Body top = 4 scaled px

SelectDir:
First row top = 8 scaled px
First row -> secondary instruction = 16 scaled px
Secondary instruction -> path field = 8 scaled px
```

Only InfoBefore differs from SelectDir because their content density is
different.

Do not force identical body offsets across pages merely for parameter symmetry.

If R10.2 is visually accepted, freeze the installer body layout.
